@model CultureDemo.Models.Case.CaseBO
@using CultureDemo.Languages

@{
    ViewData["Title"] = "Create Case";
}

<div class="container">
    <h1>@Resources.CreateCaseTitle</h1>
    @using (Html.BeginForm("Create", "Case", FormMethod.Post))
    {
        <div class="row mb-3">
            <div class="col">
                @Html.LabelFor(e => e.Case_Name)
                @Html.TextBoxFor(e => e.Case_Name, null, new { @placeholder = $"{@Resources.EnterCaseName}", @class = "form-control" })
                @Html.ValidationMessageFor(e => e.Case_Name)
            </div>
        </div>
        <div class="row mb-3">
            <div class="col">
                @Html.LabelFor(e => e.Case_Description)
                @Html.TextAreaFor(e => e.Case_Description, 5, 10, new { @placeholder = $"{@Resources.EnterCaseDescription}", @class = "form-control" })
                @Html.ValidationMessageFor(e => e.Case_Description)
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-6">
                @Html.LabelFor(e => e.User_FirstName)
                @Html.TextBoxFor(e => e.User_FirstName, null, new { @placeholder = $"{@Resources.EnterFirstName}", @class = "form-control" })
                @Html.ValidationMessageFor(e => e.User_FirstName)
            </div>
            <div class="col-6">
                @Html.LabelFor(e => e.User_LastName)
                @Html.TextBoxFor(e => e.User_LastName, null, new { @placeholder = $"{@Resources.EnterLastName}", @class = "form-control" })
                @Html.ValidationMessageFor(e => e.User_LastName)
            </div>
        </div>
        @*DEMO: 3*@
        <input type="submit" value="@Resources.Submit" class="btn btn-primary mt-2">
        @Html.ActionLink(@Resources.Cancel, "Index", "Case", null, new { @class="btn btn-danger mt-2" })
    }
</div>

